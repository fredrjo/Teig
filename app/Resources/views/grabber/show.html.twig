{% extends 'base.html.twig' %}

{% block body %}
  <h1>Grabber</h1>
  <div class="container">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ grabber.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ grabber.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="list-inline">
        <li>
            <a href="{{ path('grabber_edit', { 'id': grabber.id }) }}" class="btn btn-info" role="button">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger" role="button">
            {{ form_end(delete_form) }}
        </li>
        <li>
            <a href="{{ path('grabber_index') }}">Back to the list</a>
        </li>
    </ul>
  </div>
  <div>
{% for meters in metersArray %}
{% include 'meter/index.html.twig' %}
{% endfor %}
  </div>



{% endblock %}
